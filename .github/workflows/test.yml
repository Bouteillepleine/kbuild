name: OnePlus ReSukiSU

on:
  workflow_dispatch:
    inputs:
      FILE:
        type: choice
        description: "Configuration file"
        required: true
        default: oneplus_15_w
        options:
          - oneplus_15_w

      MANAGER_SOURCE:
        type: choice
        description: "Manager invocation source"
        required: true
        default: MD3
        options:
          - MD3
          - MD3_SPOOF

      KPM:
        type: choice
        description: "Kernel module implementation method"
        required: true
        default: KPM
        options:
          - KPM
          - KPN
          - NoN

      SUSFS_META:
        type: string
        description: "Roll back SUSFS to a specific hash version"
        required: false
        default: ""

      DYNAMIC_REPO:
        type: string
        description: "Owner of the dynamic manifest repository"
        required: true
        default: "Bouteillepleine"

      BUILD_TIME:
        type: string
        description: "Custom build time (enter F to use current UTC time)"
        required: false
        default: "Wed Feb 11 11:11:11 UTC 2026"

      KSU_META:
        type: string
        description: "Branch name (required) / custom version tag (leave empty to keep) / rollback branch commit hash (leave empty to keep)"
        required: false
        default: "main/⚡Ultra⚡/"

      SUFFIX:
        type: string
        description: "Custom kernel suffix (leave empty to use a random string)"
        required: false
        default: "5-ga5f232d1ead0-ab14083253-4k"

      SUBLEVEL:
        type: string
        description: "Custom kernel level spoofing (SUBLEVEL)"
        required: false
        default: ""

      LSM:
        type: boolean
        description: "Enable critical partition write-protection module (BBG)?"
        required: true
        default: true

      NETFILTER:
        type: boolean
        description: "Enable network feature extensions (NETFILTER)?"
        required: true
        default: true

      BBR_ECN:
        type: boolean
        description: "Enable network optimization algorithms (BBR+ECN)?"
        required: true
        default: true

      UNICODE_BYPASS:
        type: boolean
        description: "Add Unicode invisible-codepoint bypass for the read-vulnerability fix?"
        required: true
        default: false

      XUS_FIX:
        type: boolean
        description: "Prevent generation of 5US error files?"
        required: true
        default: false

      SUSFS_DEV:
        type: boolean
        description: "Fetch the SUSFS-DEV branch?"
        required: true
        default: false

      SPACE_NOCLEAN:
        type: boolean
        description: "Disable disk space cleanup?"
        required: true
        default: false

      BUILD_NOCCACHE:
        type: boolean
        description: "Disable ccache for this build?"
        required: true
        default: false

jobs:
  build:
    name: ${{ github.event.inputs.KPM == 'KPM' && '[KPM] ' || github.event.inputs.KPM == 'KPN' && '[KPN] ' || '' }}${{ github.event.inputs.LSM == 'true' && '[LSM] ' || '' }}For ${{ github.event.inputs.FILE }} ${{ github.event.inputs.SUFFIX }}
    runs-on: ubuntu-latest

    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"
      CCACHE_MAXSIZE: 8G

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Record Build Start Time
        shell: bash
        run: |
          echo "BUILD_START_TIME=$(date +%s)" >> "$GITHUB_ENV"

      - name: Pre-build Space Boost (aggressive)
        if: ${{ github.event.inputs.SPACE_NOCLEAN == 'false' }}
        shell: bash
        run: |
          set -euxo pipefail

          echo "::group::Disk before cleanup"
          df -h
          echo "::endgroup::"

          echo "::group::Remove large preinstalled SDKs/caches"
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf /usr/share/dotnet || true
          sudo rm -rf /opt/ghc || true
          sudo rm -rf /usr/local/.ghcup || true
          sudo rm -rf /opt/hostedtoolcache/CodeQL || true
          sudo rm -rf /opt/hostedtoolcache/go || true
          sudo rm -rf /opt/hostedtoolcache/Ruby || true
          sudo rm -rf /opt/hostedtoolcache/Python || true
          sudo rm -rf "$HOME/.cache" || true
          sudo rm -rf /var/lib/apt/lists/* || true
          sudo rm -rf /var/cache/apt/* || true
          echo "::endgroup::"

          echo "::group::Disk after cleanup"
          df -h
          echo "::endgroup::"

      - name: Maximize Build Space
        if: ${{ github.event.inputs.SPACE_NOCLEAN == 'false' }}
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          remove-dotnet: "true"
          remove-android: "true"
          remove-haskell: "true"
          remove-codeql: "true"

      - name: Install Dependencies
        shell: bash
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            python3 git curl jq ccache libelf-dev build-essential flex bison libssl-dev \
            libncurses-dev liblz4-tool zlib1g-dev libxml2-utils rsync unzip gawk dos2unix \
            kmod libdw-dev elfutils zip
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/* /var/cache/apt/* || true
          df -h

      - name: Extract Manifest Info
        id: extract_info
        env:
          FILE: ${{ github.event.inputs.FILE }}
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -e
          cd "$GITHUB_WORKSPACE"

          if [[ "$FILE" =~ ^(.+)_([a-zA-Z])$ ]]; then
            FILE_CONF="${BASH_REMATCH[1]}"
          else
            FILE_CONF="$FILE"
          fi

          FILE_BASE=$(echo "$FILE_CONF" | sed -E 's/_aosp/(AOSP)/g; s/_([a-zA-Z0-9])/\U\1/g; s/^oneplus/OnePlus/; s/^realme/RealME/; s/^oppo/OPPO/')
          mkdir -p ".repo/manifests_fallback"
          XML_PATH=".repo/manifests_fallback/${FILE}.xml"
          README_PATH=".repo/manifests_fallback/README.md"

          echo "Processing FILE=$FILE, CONF=$FILE_CONF, BASE=$FILE_BASE"
          echo "FILE_CONF=$FILE_CONF" >> "$GITHUB_ENV"
          echo "FILE_BASE=$FILE_BASE" >> "$GITHUB_ENV"

          DYNAMIC_REPO_INPUT="${{ github.event.inputs.DYNAMIC_REPO }}"
          declare -A REPOS
          REPOS["Bouteillepleine"]="Bouteillepleine|kernel_manifest"
          REPOS["Appendix"]="Bouteillepleine|Kernel_Manifest_Appendix"
          REPOS["Dynamic"]="${DYNAMIC_REPO_INPUT}|kernel_manifest"

          FOUND_REPO=""
          FOUND_REPO_NAME=""
          FOUND_BRANCH=""

          check_repo() {
            local ENTRY=$1
            local OWNER=${ENTRY%%|*}
            local REPO_NAME=${ENTRY##*|}
            local API_URL="https://api.github.com/repos/${OWNER}/${REPO_NAME}"

            echo "Trying to fetch branch list from ${OWNER}/${REPO_NAME}..."
            BRANCHES=$(
              curl -fsL \
                -H "Authorization: Bearer ${GH_TOKEN}" \
                -H "X-GitHub-Api-Version: 2022-11-28" \
                "$API_URL/branches" \
              | jq -r 'if type=="array" then .[].name else empty end' 2>/dev/null || true
            )

            if [[ -z "$BRANCHES" ]]; then
              echo "Failed to fetch branch list for ${OWNER}/${REPO_NAME}."
              return 1
            fi

            for BRANCH in $BRANCHES; do
              XML_URL="https://raw.githubusercontent.com/${OWNER}/${REPO_NAME}/${BRANCH}/${FILE}.xml"
              README_URL="https://raw.githubusercontent.com/${OWNER}/${REPO_NAME}/${BRANCH}/README.md"

              if curl -sf --head "$XML_URL" >/dev/null; then
                echo "Found ${FILE}.xml in ${OWNER}/${REPO_NAME} (branch: ${BRANCH})"
                curl -s -o "$XML_PATH" "$XML_URL"
                curl -s -o "$README_PATH" "$README_URL" || true

                FOUND_REPO="$OWNER"
                FOUND_REPO_NAME="$REPO_NAME"
                FOUND_BRANCH="$BRANCH"
                return 0
              fi
            done

            return 1
          }

          if ! check_repo "${REPOS["Bouteillepleine"]}"; then
            if ! check_repo "${REPOS["Appendix"]}"; then
              check_repo "${REPOS["Dynamic"]}"
            fi
          fi

          if [[ -z "$FOUND_REPO" || ! -s "$XML_PATH" ]]; then
            echo "Unable to find ${FILE}.xml in any repository"
            exit 9
          fi

          echo "MANIFEST_REPO=$FOUND_REPO" >> "$GITHUB_ENV"
          echo "MANIFEST_REPO_NAME=$FOUND_REPO_NAME" >> "$GITHUB_ENV"
          echo "MANIFEST_BRANCH=$FOUND_BRANCH" >> "$GITHUB_ENV"

          REVISION=$(grep -oP '<project[^>]+revision="\K[^"]+' "$XML_PATH" | head -n1 || true)
          echo "Parsing FILE to obtain CPU and ANDROID_VERSION..."

          CPU=$(echo "$REVISION" | sed -E 's#^(oneplus|realme|oppo)/([^_]+).*#\2#')
          ANDROID_VERSION=$(echo "$REVISION" | grep -oP '\d{1,2}\.\d{1,2}(\.\d{1,2})?')

          if [[ -n "$CPU" && -n "$ANDROID_VERSION" ]]; then
            echo "CPU=$CPU, ANDROID_VERSION=$ANDROID_VERSION"
            echo "CPU=$CPU" >> "$GITHUB_ENV"
            echo "ANDROID_VERSION=$ANDROID_VERSION" >> "$GITHUB_ENV"
            echo "ANDROID_SHORT_VERSION=${ANDROID_VERSION%%.*}" >> "$GITHUB_ENV"
          else
            echo "Unable to extract CPU or ANDROID_VERSION from revision"
            exit 1
          fi

          echo "Parsing README.md to obtain CPUD and BUILD_METHOD..."
          if [[ -s "$README_PATH" ]]; then
            BUILD_LINE=$(grep -m1 'oplus_build_kernel.sh' "$README_PATH" || true)
            if [[ -n "$BUILD_LINE" ]]; then
              CPUD=$(echo "$BUILD_LINE" | awk '{print $(NF-1)}')
              BUILD_METHOD=$(echo "$BUILD_LINE" | awk '{print $NF}')
              echo "CPUD=$CPUD, BUILD_METHOD=$BUILD_METHOD"
              echo "CPUD=$CPUD" >> "$GITHUB_ENV"
              echo "BUILD_METHOD=$BUILD_METHOD" >> "$GITHUB_ENV"
            else
              echo "Build command not found in README.md"
            fi
          else
            echo "README.md download failed or is empty"
          fi

          if [[ -z "${CPUD:-}" ]]; then
            CPUD="canoe"
            echo "CPUD=$CPUD" >> "$GITHUB_ENV"
          fi
          if [[ -z "${BUILD_METHOD:-}" ]]; then
            BUILD_METHOD="perf"
            echo "BUILD_METHOD=$BUILD_METHOD" >> "$GITHUB_ENV"
          fi

          echo "Parsing manifest to obtain PATH_2..."
          PATH_2=""
          if [[ "$CPU" == sm* ]]; then
            COUNT_SM=0
            COUNT_MASTER=0
            while read -r LINE; do
              REV=$(echo "$LINE" | grep -oP 'revision="\K[^"]+' || true)
              PATH_VAL=$(echo "$LINE" | grep -oP 'path="\K[^"]+' || true)
              if [[ "$REV" =~ ^(oneplus|oppo)/ ]] && [[ -n "$PATH_VAL" ]]; then
                COUNT_SM=$((COUNT_SM + 1))
                if [[ $COUNT_SM -eq 2 ]]; then
                  PATH_2="${PATH_VAL#kernel_platform/}"
                  break
                fi
              elif [[ "$REV" == "master" ]] && [[ -n "$PATH_VAL" ]]; then
                COUNT_MASTER=$((COUNT_MASTER + 1))
                if [[ $COUNT_MASTER -eq 2 ]]; then
                  PATH_2="${PATH_VAL#kernel_platform/}"
                  break
                fi
              fi
            done < <(grep "<project " "$XML_PATH" || true)
          fi

          if [[ -n "$PATH_2" ]]; then
            echo "PATH_2=$PATH_2"
            echo "PATH_2=$PATH_2" >> "$GITHUB_ENV"
          else
            echo "No matching PATH_2 found; skipping."
          fi

          echo "value=${FILE_BASE}_Android${ANDROID_VERSION}" >> "$GITHUB_OUTPUT"

      - name: Debug Show Selected Inputs
        shell: bash
        run: |
          echo "-----------------------"
          echo "Selected CPU: ${{ env.CPU }}"
          echo "Selected CPUD: ${{ env.CPUD }}"
          echo "Selected ANDROID_VERSION: ${{ env.ANDROID_VERSION }}"
          echo "Selected BUILD_METHOD: ${{ env.BUILD_METHOD }}"
          echo "Selected FILE: ${{ github.event.inputs.FILE }}"
          echo "Selected MANAGER_SOURCE: ${{ github.event.inputs.MANAGER_SOURCE }}"
          echo "Selected KPM: ${{ github.event.inputs.KPM }}"
          echo "Custom SUSFS_META: ${{ github.event.inputs.SUSFS_META }}"
          echo "Custom KSU_META: ${{ github.event.inputs.KSU_META }}"
          echo "Custom BUILD_TIME: ${{ github.event.inputs.BUILD_TIME }}"
          echo -n "BUILD_TIME Unicode: "
          python3 -c "import unicodedata; print(''.join(f'\033[31mU+{ord(c):04X}\033[0m ' if c.isspace() or unicodedata.category(c)=='Cf' else f'U+{ord(c):04X} ' for c in '''${{ github.event.inputs.BUILD_TIME }}'''))"
          echo "Custom SUFFIX: ${{ github.event.inputs.SUFFIX }}"
          echo -n "SUFFIX Unicode: "
          python3 -c "import unicodedata; print(''.join(f'\033[31mU+{ord(c):04X}\033[0m ' if c.isspace() or unicodedata.category(c)=='Cf' else f'U+{ord(c):04X} ' for c in '''${{ github.event.inputs.SUFFIX }}'''))"
          echo "Selected LSM: ${{ github.event.inputs.LSM }}"
          echo "Selected NETFILTER: ${{ github.event.inputs.NETFILTER }}"
          echo "Selected BBR_ECN: ${{ github.event.inputs.BBR_ECN }}"
          echo "Selected SUSFS_DEV: ${{ github.event.inputs.SUSFS_DEV }}"
          echo "Selected UNICODE_BYPASS: ${{ github.event.inputs.UNICODE_BYPASS }}"
          echo "Selected XUS_FIX: ${{ github.event.inputs.XUS_FIX }}"
          echo "Selected SPACE_NOCLEAN: ${{ github.event.inputs.SPACE_NOCLEAN }}"
          echo "Selected BUILD_NOCCACHE: ${{ github.event.inputs.BUILD_NOCCACHE }}"
          echo "-----------------------"

      - name: Check Disk Space
        shell: bash
        run: df -h

      - name: Set Cache Environment
        if: ${{ github.event.inputs.BUILD_NOCCACHE == 'false' }}
        shell: bash
        run: |
          set -euxo pipefail
          echo "CCACHE_DIR=${GITHUB_WORKSPACE}/.ccache_${{ github.event.inputs.FILE }}" >> "$GITHUB_ENV"
          mkdir -p "${GITHUB_WORKSPACE}/.ccache_${{ github.event.inputs.FILE }}"
          echo "CCACHE_DIR=${GITHUB_WORKSPACE}/.ccache_${{ github.event.inputs.FILE }}"

      - name: Configure Git
        shell: bash
        run: |
          git config --global user.name "OnePlus"
          git config --global user.email "OnePlus@qq.com"

      - name: Restore Ccache
        if: ${{ github.event.inputs.BUILD_NOCCACHE == 'false' }}
        uses: actions/cache@v3
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-${{ runner.os }}-${{ github.ref_name }}-${{ github.event.inputs.FILE }}-${{ env.BUILD_METHOD }}-16
          restore-keys: |
            ccache-${{ runner.os }}-${{ github.ref_name }}-${{ github.event.inputs.FILE }}-
            ccache-${{ runner.os }}-${{ github.event.inputs.FILE }}-

      - name: Initialize Ccache
        if: ${{ github.event.inputs.BUILD_NOCCACHE == 'false' }}
        shell: bash
        run: |
          INIT_FLAG="${{ env.CCACHE_DIR }}/.ccache_initialized"
          if command -v ccache >/dev/null 2>&1; then
            if [ ! -f "$INIT_FLAG" ]; then
              echo "Initializing ccache (${{ env.CCACHE_DIR }})..."
              mkdir -p "${{ env.CCACHE_DIR }}"
              ccache -M ${{ env.CCACHE_MAXSIZE }}
              touch "$INIT_FLAG"
              echo "ccache initialization completed"
            else
              echo "ccache is already initialized, skipping"
            fi
          else
            echo "ccache not installed, skipping"
          fi
          ccache -s || true

      - name: Install Repo Tool
        shell: bash
        run: |
          curl -fsSL https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
          chmod a+x ~/repo
          sudo mv ~/repo /usr/local/bin/repo

      - name: Fail fast if disk too low (before repo sync)
        shell: bash
        run: |
          set -euxo pipefail
          df -h
          AVAIL_GB=$(df -BG "$GITHUB_WORKSPACE" | awk 'NR==2{gsub("G","",$4); print $4}')
          if [ "${AVAIL_GB:-0}" -lt 15 ]; then
            echo "ERROR: Only ${AVAIL_GB}G free on workspace disk. Aborting to avoid runner crash."
            exit 1
          fi

      - name: Initialize Repo and Sync
        env:
          FILE: ${{ github.event.inputs.FILE }}
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p kernel_workspace && cd kernel_workspace

          mkdir -p .repo/manifests
          cp "$GITHUB_WORKSPACE/.repo/manifests_fallback/${FILE}.xml" ".repo/manifests/${FILE}.xml"
          BASE_URL="https://github.com/${MANIFEST_REPO}/${MANIFEST_REPO_NAME}.git"

          echo "Initializing repo using $MANIFEST_REPO/$MANIFEST_REPO_NAME ($MANIFEST_BRANCH)..."
          repo init -u "$BASE_URL" -b "$MANIFEST_BRANCH" -m "${FILE}.xml" --depth=1 --no-clone-bundle --no-tags
          repo sync -c -j8 --prune --no-clone-bundle --no-tags --force-sync

          for dir in kernel_platform/common kernel_platform/msm-kernel; do
            if [ -e "$dir/BUILD.bazel" ]; then
              sed -i '/^[[:space:]]*"protected_exports_list"[[:space:]]*:[[:space:]]*"android\/abi_gki_protected_exports_aarch64",$/d' "$dir/BUILD.bazel"
            fi
            rm -f "$dir/android/abi_gki_protected_exports_"* 2>/dev/null || true
          done

      - name: Kernel Version Handling
        shell: bash
        run: |
          set -euo pipefail
          cd kernel_workspace/kernel_platform

          BRANCH=""
          for f in ./common/build.config.constants ./common/build.config.common; do
            if [ -f "$f" ]; then
              BRANCH=$(grep -m1 '^BRANCH=' "$f" | cut -d= -f2 || true)
              [ -n "$BRANCH" ] && break
            fi
          done

          if [[ -z "$BRANCH" && -n "${PATH_2:-}" ]]; then
            if [ -f "./$PATH_2/android/ACK_SHA" ]; then
              BRANCH=$(sed -n '2p' "./$PATH_2/android/ACK_SHA" | awk -F'-' '{print $1"-"$2}')
            fi
          fi

          if [ -n "$BRANCH" ]; then
            echo "KANDROID_VERSION=${BRANCH%-*}" >> "$GITHUB_ENV"
            echo "KERNEL_VERSION=${BRANCH#*-}" >> "$GITHUB_ENV"
          else
            echo "No BRANCH found in build.config* and ACK_SHA files"
          fi

          SUBLEVEL_INPUT="${{ github.event.inputs.SUBLEVEL }}"
          if [ -n "$SUBLEVEL_INPUT" ]; then
            sed -i "s/^\(SUBLEVEL[[:space:]]*=[[:space:]]*\).*/\1$SUBLEVEL_INPUT/" ./common/Makefile
          fi

          NEW_VERSION=$(awk '/^VERSION =/ {v=$3} /^PATCHLEVEL =/ {p=$3} /^SUBLEVEL =/ {s=$3} END {print v"."p"."s}' ./common/Makefile)
          echo "TKERNEL_VERSION=$NEW_VERSION" >> "$GITHUB_ENV"

          if [ -n "$BRANCH" ]; then
            KV="${BRANCH#*-}"
            echo "KV1=${KV%%.*}" >> "$GITHUB_ENV"
            echo "KV2=$(echo "$KV" | tr -d '.')" >> "$GITHUB_ENV"
          fi
          echo "KV3=$(echo "$NEW_VERSION" | tr -d '.')" >> "$GITHUB_ENV"

      - name: Custom Kernel Suffix if set
        if: ${{ github.event.inputs.SUFFIX != '' }}
        shell: bash
        run: |
          set -euo pipefail
          cd kernel_workspace
          SUFFIX="${{ github.event.inputs.SUFFIX }}"
          KANDROID_VERSION="${{ env.KANDROID_VERSION }}"

          for path in \
            kernel_platform/common/scripts/setlocalversion \
            kernel_platform/msm-kernel/scripts/setlocalversion \
            kernel_platform/external/dtc/scripts/setlocalversion; do

            [ -f "$path" ] || continue
            echo "Modifying: $path"

            sed -i 's/ -dirty//g' "$path"
            sed -i '$i res=$(echo "$res" | sed '\''s/-dirty//g'\'')' "$path"

            if grep -q 'KERNELVERSION.*scm_version' "$path"; then
              sed -i "s|echo \"\${KERNELVERSION}.*scm_version}\"|echo \"\${KERNELVERSION}-${KANDROID_VERSION}-${SUFFIX}\"|" "$path"
            elif grep -q 'echo "\$res"' "$path"; then
              tac "$path" | sed "0,/echo \"\\\$res\"/s//res=\\\$(echo \\\$res | cut -d- -f1-2)-${SUFFIX}; echo \"\\\$res\";/" | tac > "$path.tmp" && mv "$path.tmp" "$path"
            else
              echo "echo \"\$res-${SUFFIX}\"" >> "$path"
            fi

            chmod +x "$path"
          done

          git add -A
          git commit -m "Custom suffix & removed -dirty" || true

      - name: Custom Kernel Random Suffix if empty
        if: ${{ github.event.inputs.SUFFIX == '' }}
        shell: bash
        run: |
          set -euo pipefail
          cd kernel_workspace
          KANDROID_VERSION="${{ env.KANDROID_VERSION }}"

          RANDOM_DIGIT=$(od -An -N1 -tu1 < /dev/urandom | tr -d '[:space:]' | awk '{print $1 % 11}')
          RANDOM_HASH=$(od -An -N7 -tx1 /dev/urandom | tr -d ' \n')
          RANDOM_SUFFIX="${RANDOM_DIGIT}-o-g${RANDOM_HASH}"

          for path in \
            kernel_platform/common/scripts/setlocalversion \
            kernel_platform/msm-kernel/scripts/setlocalversion \
            kernel_platform/external/dtc/scripts/setlocalversion; do

            [ -f "$path" ] || continue
            echo "Modifying: $path"

            sed -i 's/ -dirty//g' "$path"
            sed -i '$i res=$(echo "$res" | sed '\''s/-dirty//g'\'')' "$path"

            if grep -q 'KERNELVERSION.*scm_version' "$path"; then
              sed -i "s|echo \"\${KERNELVERSION}.*scm_version}\"|echo \"\${KERNELVERSION}-${KANDROID_VERSION}-${RANDOM_SUFFIX}\"|" "$path"
            elif grep -q 'echo "\$res"' "$path"; then
              tac "$path" | sed "0,/echo \"\\\$res\"/s//res=\\\$(echo \\\$res | cut -d- -f1-2)-${RANDOM_SUFFIX}; echo \"\\\$res\";/" | tac > "$path.tmp" && mv "$path.tmp" "$path"
            else
              echo "echo \"\$res-${RANDOM_SUFFIX}\"" >> "$path"
            fi

            chmod +x "$path"
          done

          git add -A
          git commit -m "Random suffix & remove -dirty" || true

      - name: Resolve Manager Source
        id: manager
        shell: bash
        run: |
          set -euo pipefail
          MANAGER_SOURCE="${{ github.event.inputs.MANAGER_SOURCE }}"

          case "$MANAGER_SOURCE" in
            MD3_SPOOF)
              BRANCH="main"
              ARTIFACT="Spoofed-Manager-release"
              ;;
            MD3|*)
              BRANCH="main"
              ARTIFACT="Manager-release"
              ;;
          esac

          echo "manager_branch=$BRANCH" >> "$GITHUB_OUTPUT"
          echo "manager_artifact=$ARTIFACT" >> "$GITHUB_OUTPUT"

      - name: Add ReSukiSU
        shell: bash
        run: |
          set -euo pipefail
          cd kernel_workspace/kernel_platform
          MANAGER_BRANCH="${{ steps.manager.outputs.manager_branch }}"
          KSU_META="${{ github.event.inputs.KSU_META }}"

          if [[ "$(grep -o '/' <<< "$KSU_META" | wc -l)" -lt 2 ]]; then
            echo "Error: KSU_META is missing required '/' separators. Format: branch/custom_tag(optional)/commit_hash(optional)"
            exit 10
          fi

          IFS='/' read -r BRANCH_NAME KSU_CUSTOM_TAG MANUAL_HASH <<< "$KSU_META"

          curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/${MANAGER_BRANCH}/kernel/setup.sh" | bash -s "$BRANCH_NAME"
          cd ./KernelSU

          if [[ -n "$KSU_CUSTOM_TAG" ]]; then
            echo "KSU_CUSTOM_TAG=$KSU_CUSTOM_TAG" >> "$GITHUB_ENV"
          fi

          if [[ -n "$MANUAL_HASH" ]]; then
            git fetch origin "$BRANCH_NAME" --depth=50
            git checkout "$MANUAL_HASH"
          fi

          KSU_COMMIT_SHA="$(git rev-parse HEAD || true)"
          echo "KSU_COMMIT_SHA=$KSU_COMMIT_SHA" >> "$GITHUB_ENV"

          KSU_VERSION=$(expr $(git rev-list --count "$MANAGER_BRANCH" 2>/dev/null || echo 13000) + 30700)
          echo "KSUVER=$KSU_VERSION" >> "$GITHUB_ENV"

      - name: Apply Patches ReSukiSU
        env:
          FILE: ${{ github.event.inputs.FILE }}
        shell: bash
        run: |
          set -euo pipefail
          cd kernel_workspace

          if [ "${{ github.event.inputs.SUSFS_META }}" != "-1" ]; then
            SUSFS_META="${{ github.event.inputs.SUSFS_META }}"
            git clone https://gitlab.com/simonpunk/susfs4ksu.git -b "gki-${{ env.KANDROID_VERSION }}-${{ env.KERNEL_VERSION }}${{ github.event.inputs.SUSFS_DEV == 'true' && '-dev' || '' }}"
            cd susfs4ksu
            if [ -n "$SUSFS_META" ]; then
              git checkout "$SUSFS_META"
              echo "Using SUSFS_META: $SUSFS_META"
            else
              echo "SUSFS_META is empty; using latest upstream SUSFS"
            fi
            SUSFS_COMMIT_SHA="$(git rev-parse HEAD || true)"
            echo "SUSFS_COMMIT_SHA=$SUSFS_COMMIT_SHA" >> "$GITHUB_ENV"
            cd ..
          fi

          git clone https://github.com/ShirkNeko/SukiSU_patch.git
          git clone https://github.com/Numbersf/Action-Build.git

          cd kernel_platform
          if [ "${{ github.event.inputs.SUSFS_META }}" != "-1" ]; then
            cp ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-${{ env.KANDROID_VERSION }}-${{ env.KERNEL_VERSION }}.patch ./common/
            cp ../susfs4ksu/kernel_patches/fs/* ./common/fs/
            cp ../susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/
          fi

          cd ./common
          GKI_V="${{ env.KANDROID_VERSION }}-${{ env.KERNEL_VERSION }}"
          SUBLEVEL=$(grep '^SUBLEVEL *=' Makefile | head -n1 | cut -d= -f2 | tr -d ' ')

          if [ "$GKI_V" == "android13-5.15" ] && [ "$SUBLEVEL" -lt 123 ]; then
            cp ../../Action-Build/patches/fix_5.15.legacy ./fix_5.15.legacy.patch
            patch -p1 < fix_5.15.legacy.patch
          fi

          if [ "${{ github.event.inputs.SUSFS_META }}" != "-1" ]; then
            fake_patched=0

            if [ "$GKI_V" = "android15-6.6" ]; then
              if ! grep -qxF $'\tunsigned int nr_subpages = __PAGE_SIZE / PAGE_SIZE;' ./fs/proc/task_mmu.c; then
                sed -i -e '/int ret = 0, copied = 0;/a \\tunsigned int nr_subpages \= __PAGE_SIZE \/ PAGE_SIZE;' -e '/int ret = 0, copied = 0;/a \\tpagemap_entry_t \*res = NULL;' ./fs/proc/task_mmu.c
                fake_patched=1
              fi
            fi

            patch -p1 < 50_add_susfs_in_gki-${{ env.KANDROID_VERSION }}-${{ env.KERNEL_VERSION }}.patch || true

            if [ "$fake_patched" = 1 ]; then
              if [ "$GKI_V" = "android15-6.6" ]; then
                sed -i -e '/unsigned int nr_subpages \= __PAGE_SIZE \/ PAGE_SIZE;/d' -e '/pagemap_entry_t \*res = NULL;/d' ./fs/proc/task_mmu.c || true
              fi
            fi

            if [ "${{ github.event.inputs.XUS_FIX }}" = "true" ]; then
              cp ../../Action-Build/patches/No_5uS.patch ./
              patch -p1 < No_5uS.patch || true
            fi
          fi

      - name: Apply Unicode Bypass
        if: ${{ github.event.inputs.UNICODE_BYPASS == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          PATCH_DIR="${GITHUB_WORKSPACE}/kernel_workspace/Action-Build/patches"
          cd kernel_workspace/kernel_platform/common
          if [ "${KV1:-0}" -lt 6 ]; then
            patch -p1 --forward < "${PATCH_DIR}/unicode_bypass_fix_6.1-.patch" || true
          else
            patch -p1 --forward < "${PATCH_DIR}/unicode_bypass_fix_6.1+.patch" || true
          fi

      - name: Apply LSM_BBG
        if: ${{ github.event.inputs.LSM == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          cd kernel_workspace/kernel_platform/common
          curl -LSs https://raw.githubusercontent.com/vc-teahouse/Baseband-guard/main/setup.sh | bash -s fix_blkdev_rename
          sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/selinux/selinux,baseband_guard/ } }' ./security/Kconfig

      - name: Add Configuration Settings
        shell: bash
        run: |
          set -euo pipefail
          cd kernel_workspace/kernel_platform/common
          CONFIG_FILE=./arch/arm64/configs/gki_defconfig
          MANAGER_BRANCH="${{ steps.manager.outputs.manager_branch }}"

          echo "CONFIG_KSU=y" >> "$CONFIG_FILE"
          echo "CONFIG_KSU_MULTI_MANAGER_SUPPORT=y" >> "$CONFIG_FILE"
          if [[ -n "${KSU_CUSTOM_TAG:-}" ]]; then
            echo "CONFIG_KSU_FULL_NAME_FORMAT=\"%TAG_NAME%-${KSU_CUSTOM_TAG}@${MANAGER_BRANCH}[%COMMIT_SHA%]\"" >> "$CONFIG_FILE"
          fi

          if [ "${{ github.event.inputs.KPM }}" = "KPM" ] || [ "${{ github.event.inputs.KPM }}" = "NoN" ]; then
            echo "CONFIG_KPM=y" >> "$CONFIG_FILE"
          fi
          if [ "${{ github.event.inputs.KPM }}" = "KPN" ]; then
            echo "CONFIG_KPM=n" >> "$CONFIG_FILE"
          fi

          if [ "${{ github.event.inputs.SUSFS_META }}" != "-1" ]; then
            echo "CONFIG_KSU_SUSFS=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SUS_MAP=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> "$CONFIG_FILE"
          else
            echo "CONFIG_KSU_SUSFS=n" >> "$CONFIG_FILE"
          fi

          echo "CONFIG_TMPFS_XATTR=y" >> "$CONFIG_FILE"
          echo "CONFIG_TMPFS_POSIX_ACL=y" >> "$CONFIG_FILE"

          echo "CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y" >> "$CONFIG_FILE"
          echo "CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE_O3=n" >> "$CONFIG_FILE"
          echo "CONFIG_WQ_POWER_EFFICIENT_DEFAULT=y" >> "$CONFIG_FILE"
          echo "CONFIG_OPTIMIZE_INLINING=y" >> "$CONFIG_FILE"

          echo "CONFIG_DEBUG_KERNEL=n" >> "$CONFIG_FILE"
          echo "CONFIG_DEBUG_INFO=n" >> "$CONFIG_FILE"
          echo "CONFIG_DEBUG_INFO_DWARF4=n" >> "$CONFIG_FILE"
          echo "CONFIG_LOCK_DEBUGGING=n" >> "$CONFIG_FILE"
          echo "CONFIG_DEBUG_MUTEXES=n" >> "$CONFIG_FILE"
          echo "CONFIG_DEBUG_SPINLOCK=n" >> "$CONFIG_FILE"
          echo "CONFIG_DEBUG_ATOMIC_SLEEP=n" >> "$CONFIG_FILE"
          echo "CONFIG_GDB_SCRIPTS=n" >> "$CONFIG_FILE"

          if [ "${{ github.event.inputs.BBR_ECN }}" = "true" ]; then
            echo "CONFIG_TCP_CONG_ADVANCED=y" >> "$CONFIG_FILE"
            echo "CONFIG_TCP_CONG_BBR=y" >> "$CONFIG_FILE"
            echo "CONFIG_NET_SCH_FQ=y" >> "$CONFIG_FILE"
            echo "CONFIG_TCP_CONG_BIC=n" >> "$CONFIG_FILE"
            echo "CONFIG_TCP_CONG_WESTWOOD=n" >> "$CONFIG_FILE"
            echo "CONFIG_TCP_CONG_HTCP=n" >> "$CONFIG_FILE"
            echo "CONFIG_IP_ECN=y" >> "$CONFIG_FILE"
            echo "CONFIG_TCP_ECN=y" >> "$CONFIG_FILE"
            echo "CONFIG_IPV6_ECN=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_NF_TARGET_ECN=y" >> "$CONFIG_FILE"
          fi

          if [ "${{ github.event.inputs.LSM }}" = "true" ]; then
            echo "CONFIG_BBG=y" >> "$CONFIG_FILE"
          fi

          if [[ "${CPU}" == sm* && "${{ github.event.inputs.NETFILTER }}" == "true" ]]; then
            echo "CONFIG_BPF_STREAM_PARSER=y" >> "$CONFIG_FILE"
            echo "CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y" >> "$CONFIG_FILE"
            echo "CONFIG_NETFILTER_XT_SET=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_MAX=65534" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_BITMAP_IP=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_BITMAP_IPMAC=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_BITMAP_PORT=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_IP=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_IPMARK=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_IPPORT=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_IPPORTIP=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_IPPORTNET=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_IPMAC=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_MAC=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_NETPORTNET=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_NET=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_NETNET=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_NETPORT=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_HASH_NETIFACE=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP_SET_LIST_SET=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP6_NF_NAT=y" >> "$CONFIG_FILE"
            echo "CONFIG_IP6_NF_TARGET_MASQUERADE=y" >> "$CONFIG_FILE"
          fi

          sed -i 's/check_defconfig//' ./build.config.gki

      - name: Fix IPv6_NAT Error
        if: ${{ github.event.inputs.NETFILTER == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          PATCH_DIR="${GITHUB_WORKSPACE}/kernel_workspace/Action-Build/patches"
          cd kernel_workspace/kernel_platform/common
          if [[ "${CPU}" == sm* ]]; then
            patch -p1 -F 3 < "${PATCH_DIR}/IPv6_NAT_FIX.patch"
          else
            echo "Your device does not support IPv6 NAT. Skipping this step."
          fi

      - name: Custom BUILD_TIME
        shell: bash
        run: |
          set -euo pipefail
          INPUT_TIME="${{ github.event.inputs.BUILD_TIME }}"
          if [[ -n "$INPUT_TIME" && "$INPUT_TIME" != "F" && "$INPUT_TIME" != "f" ]]; then
            DATESTR="$INPUT_TIME"
          else
            DATESTR="$(TZ='UTC' date +'%a %b %d %T %Z %Y')"
          fi
          echo "KBUILD_BUILD_TIMESTAMP=${DATESTR}" >> "$GITHUB_ENV"
          echo "KBUILD_BUILD_VERSION=1" >> "$GITHUB_ENV"
          cd kernel_workspace/kernel_platform/
          for f in common/scripts/mkcompile_h msm-kernel/scripts/mkcompile_h; do
            if [ -f "$f" ]; then
              if grep -q 'UTS_VERSION=' "$f"; then
                perl -pi -e "s{UTS_VERSION=\"\\\$\\(.*?\\)\"}{UTS_VERSION=\"#1 SMP PREEMPT $DATESTR\"}" "$f"
              else
                perl -0777 -pi -e "s{cat <<EOF}{cat <<EOF\n#undef UTS_VERSION\n#define UTS_VERSION \"#1 SMP PREEMPT $DATESTR\" } unless /UTS_VERSION/" "$f"
              fi
            fi
          done

      - name: Modules        
        shell: bash
        run: |
          set -euo pipefail
          echo "Disabling module name protection for kernel 6.12"
          sed -i '0,/protected_module_names_list *= *":gki_aarch64_protected_module_names"/s//check_defconfig = "disabled"/' \
            kernel_workspace/kernel_platform/common/BUILD.bazel

      - name: Fail fast if disk too low (before build)
        shell: bash
        run: |
          set -euxo pipefail
          df -h
          AVAIL_GB=$(df -BG "$GITHUB_WORKSPACE" | awk 'NR==2{gsub("G","",$4); print $4}')
          if [ "${AVAIL_GB:-0}" -lt 10 ]; then
            echo "ERROR: Only ${AVAIL_GB}G free on workspace disk. Aborting to avoid runner crash."
            exit 1
          fi

      - name: Build Kernel
        shell: bash
        run: |
          set -ex
          cd kernel_workspace
          ROOT="$PWD"
      
          : "${CPUD:=canoe}"
          : "${BUILD_METHOD:=perf}"
      
          trap 'echo "Cleaning intermediate files..."; rm -rf "$ROOT/kernel_platform/common/out/"*/intermediates "$ROOT/kernel_platform/out/"*/intermediates 2>/dev/null || true; df -h / "$GITHUB_WORKSPACE"' EXIT
      
          echo "::group::Building Kernel"
          pwd
          ls -la
      
          if [ -f "kernel_platform/build/build.sh" ]; then
            cd kernel_platform
            LTO=none BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh
      
          elif [ -x "kernel_platform/tools/bazel" ]; then
            cd kernel_platform
            tools/bazel build --config=fast --lto=none //common:kernel_aarch64_dist      
          
          elif [ -f "kernel_platform/oplus/build/oplus_build_kernel.sh" ]; then
            cd "$ROOT"
            LTO=none SYSTEM_DLKM_RE_SIGN=0 BUILD_SYSTEM_DLKM=0 KMI_SYMBOL_LIST_STRICT_MODE=0 \
              ./kernel_platform/oplus/build/oplus_build_kernel.sh "${CPUD}" "${BUILD_METHOD}"
      
          else
            echo "No known build entrypoint found."
            exit 1
          fi
      
          echo "::endgroup::"
      
          echo "::group::Post-Build Cleanup"
          rm -rf "$ROOT/kernel_platform/common/out/"*/intermediates "$ROOT/kernel_platform/out/"*/intermediates 2>/dev/null || true
          df -h / "$GITHUB_WORKSPACE"
          echo "::endgroup::"

      - name: Make AnyKernel3
        shell: bash
        run: |
          set -euo pipefail
          git clone https://github.com/Bouteillepleine/AnyKernel3 --depth=1
          rm -rf ./AnyKernel3/.git
          mkdir -p kernel_workspace/kernel_platform/out/Final-Image-Find/

          image_path=$(find "./kernel_workspace/kernel_platform/common/out/" -name "Image" | head -n 1)
          if [ -z "$image_path" ]; then
            image_path=$(find "./kernel_workspace/kernel_platform/out/" -name "Image" | head -n 1)
          fi

          if [ -z "$image_path" ]; then
            echo "Image not found; build failed" >&2
            exit 1
          fi

          echo "Image file located at: $image_path"
          cp "$image_path" ./AnyKernel3/Image
          cp "$image_path" kernel_workspace/kernel_platform/out/Final-Image-Find/Image

      - name: Apply patch_linux and Replace Image
        if: ${{ github.event.inputs.KPM == 'KPM' }}
        shell: bash
        run: |
          set -euo pipefail
          PATCH_DIR="${GITHUB_WORKSPACE}/kernel_workspace/SukiSU_patch/kpm"
          OUT_DIR="${GITHUB_WORKSPACE}/kernel_workspace/kernel_platform/out/Final-Image-Find"
          cd "${OUT_DIR}"
          cp "${PATCH_DIR}/patch_linux" .
          chmod +x patch_linux
          ./patch_linux
          rm -f Image
          mv oImage Image
          cp Image "${GITHUB_WORKSPACE}/AnyKernel3/Image"

      - name: Set zip Suffix
        id: suffix
        shell: bash
        run: |
          set -euo pipefail
          echo "value=${{ github.event.inputs.KPM == 'KPM' && '_KPM' || '' }}${{ github.event.inputs.LSM == 'true' && '_LSM' || '' }}_${{ github.event.inputs.MANAGER_SOURCE }}" >> "$GITHUB_OUTPUT"

      - name: Zip AnyKernel3
        shell: bash
        run: |
          set -euo pipefail
          cd AnyKernel3
          ZIP_NAME="AnyKernel3_${{ github.event.inputs.MANAGER_SOURCE }}_${{ env.KSUVER }}_OnePlus15_${{ env.TKERNEL_VERSION }}${{ steps.suffix.outputs.value }}.zip"
          zip -r9 "${ZIP_NAME}" ./* -x "*.git*"
          echo "ZIP_NAME=${ZIP_NAME}" >> "$GITHUB_ENV"
          ls -la "${ZIP_NAME}"

      - name: Upload AnyKernel3
        uses: actions/upload-artifact@v4
        with:
          name: AnyKernel3_${{ github.event.inputs.MANAGER_SOURCE }}_${{ env.KSUVER }}_OnePlus15_${{ env.TKERNEL_VERSION }}${{ steps.suffix.outputs.value }}
          path: |
            AnyKernel3/*.zip
            AnyKernel3/Image

      - name: 📊 Calculate Build Metrics
        id: metrics
        shell: bash
        run: |
          set -euo pipefail

          if [ -n "${BUILD_START_TIME:-}" ]; then
            BUILD_END_TIME=$(date +%s)
            BUILD_DURATION=$((BUILD_END_TIME - BUILD_START_TIME))
            MINUTES=$((BUILD_DURATION / 60))
            SECONDS=$((BUILD_DURATION % 60))
            echo "BUILD_TIME=${MINUTES}m ${SECONDS}s" >> "$GITHUB_ENV"
            echo "BUILD_DURATION_RAW=${BUILD_DURATION}" >> "$GITHUB_ENV"
          else
            echo "BUILD_TIME=N/A" >> "$GITHUB_ENV"
          fi

          WARNINGS_COUNT="0"
          for log in kernel_workspace/build.log kernel_workspace/out/build.log kernel_workspace/common/out/build.log; do
            if [ -f "$log" ]; then
              WARNINGS_COUNT=$(grep -c "warning:" "$log" || echo "0")
              break
            fi
          done
          echo "WARNINGS_COUNT=${WARNINGS_COUNT}" >> "$GITHUB_ENV"

          cd AnyKernel3
          ZIP_NAME="${ZIP_NAME:-AnyKernel3.zip}"

          ZIP_SHA256=$(sha256sum "${ZIP_NAME}" | awk '{print $1}')
          ZIP_SIZE=$(du -h "${ZIP_NAME}" | awk '{print $1}')
          IMAGE_SHA256=$(sha256sum Image | awk '{print $1}')
          IMAGE_SIZE=$(du -h Image | awk '{print $1}')

          echo "ZIP_SHA256=${ZIP_SHA256}" >> "$GITHUB_ENV"
          echo "ZIP_SIZE=${ZIP_SIZE}" >> "$GITHUB_ENV"
          echo "IMAGE_SHA256=${IMAGE_SHA256}" >> "$GITHUB_ENV"
          echo "IMAGE_SIZE=${IMAGE_SIZE}" >> "$GITHUB_ENV"

      - name: 📊 Build Summary
        shell: bash
        run: |
          set -euo pipefail

          KSU_COMMIT_SHA="${KSU_COMMIT_SHA:-N/A}"
          KSUVER="${KSUVER:-N/A}"
          TKERNEL_VERSION="${TKERNEL_VERSION:-N/A}"
          KANDROID_VERSION="${KANDROID_VERSION:-N/A}"
          BUILD_TIME="${BUILD_TIME:-N/A}"
          WARNINGS_COUNT="${WARNINGS_COUNT:-N/A}"
          ZIP_NAME="${ZIP_NAME:-N/A}"
          ZIP_SHA256="${ZIP_SHA256:-N/A}"
          ZIP_SIZE="${ZIP_SIZE:-N/A}"
          IMAGE_SHA256="${IMAGE_SHA256:-N/A}"
          IMAGE_SIZE="${IMAGE_SIZE:-N/A}"

          if [ "${{ github.event.inputs.SUSFS_META }}" != "-1" ]; then
            SUSFS_ENABLED="✅ Enabled"
            SUSFS_BRANCH_NAME="gki-${{ env.KANDROID_VERSION }}-${{ env.KERNEL_VERSION }}${{ github.event.inputs.SUSFS_DEV == 'true' && '-dev' || '' }}"
            SUSFS_COMMIT_SHA="${SUSFS_COMMIT_SHA:-Latest}"
            if [ "$SUSFS_COMMIT_SHA" != "Latest" ] && [ "$SUSFS_COMMIT_SHA" != "N/A" ]; then
              SUSFS_COMMIT_LINK="[\`${SUSFS_COMMIT_SHA:0:8}\`](https://gitlab.com/simonpunk/susfs4ksu/-/commit/${SUSFS_COMMIT_SHA})"
            else
              SUSFS_COMMIT_LINK="Latest upstream"
            fi
          else
            SUSFS_ENABLED="❌ Disabled"
            SUSFS_BRANCH_NAME="N/A"
            SUSFS_COMMIT_LINK="N/A"
          fi

          KSU_REPO="ReSukiSU/ReSukiSU"
          if [ "$KSU_COMMIT_SHA" != "N/A" ] && [ -n "$KSU_COMMIT_SHA" ]; then
            KSU_COMMIT_LINK="[\`${KSU_COMMIT_SHA:0:8}\`](https://github.com/${KSU_REPO}/commit/${KSU_COMMIT_SHA})"
          else
            KSU_COMMIT_LINK="N/A"
          fi

          if [ "${{ github.event.inputs.KPM }}" = "KPM" ]; then KPM_STATUS="✅ Enabled"; else KPM_STATUS="❌ Disabled"; fi
          if [ "${{ github.event.inputs.LSM }}" = "true" ]; then LSM_STATUS="✅ Enabled"; else LSM_STATUS="❌ Disabled"; fi
          if [ "${{ github.event.inputs.BBR_ECN }}" = "true" ]; then BBR_ECN_STATUS="✅ Enabled"; else BBR_ECN_STATUS="❌ Disabled"; fi
          if [ "${{ github.event.inputs.NETFILTER }}" = "true" ]; then NETFILTER_STATUS="✅ Enabled (IPSET & IPv6 NAT)"; else NETFILTER_STATUS="❌ Disabled"; fi
          if [ "${{ github.event.inputs.UNICODE_BYPASS }}" = "true" ]; then UNICODE_FIX_STATUS="✅ Enabled"; else UNICODE_FIX_STATUS="❌ Disabled"; fi
          if [ "${{ github.event.inputs.SUSFS_DEV }}" = "true" ]; then SUSFS_DEV_MODE="✅ Yes"; else SUSFS_DEV_MODE="❌ No"; fi
          if [ "${{ github.event.inputs.BUILD_NOCCACHE }}" = "true" ]; then CCACHE_STATUS="❌ Disabled"; else CCACHE_STATUS="✅ Enabled (8GB max)"; fi

          cat >> "$GITHUB_STEP_SUMMARY" <<EOF
          # OnePlus 15 ReSukiSU Kernel Build Complete

          ## Device Information
          | Property | Value |
          |----------|-------|
          | **Device** | OnePlus 15 |
          | **Android Version** | ${KANDROID_VERSION} |
          | **Kernel Version** | ${TKERNEL_VERSION} |
          | **Configuration** | ${{ github.event.inputs.FILE }} |

          ## Build Configuration
          | Component | Version/Setting |
          |-----------|----------------|
          | **Manager Source** | ${{ github.event.inputs.MANAGER_SOURCE }} |
          | **KSU Version** | v${KSUVER} |
          | **SUSFS** | ${SUSFS_ENABLED} |
          | **SUSFS Branch** | ${SUSFS_BRANCH_NAME} |
          | **SUSFS Dev Mode** | ${SUSFS_DEV_MODE} |
          | **Ccache** | ${CCACHE_STATUS} |
          | **Build Time** | ${BUILD_TIME} |
          | **Warnings** | ${WARNINGS_COUNT} |

          ## Build Output
          | File | Details |
          |------|---------|
          | **ZIP Name** | \`${ZIP_NAME}\` |
          | **ZIP Size** | ${ZIP_SIZE} |
          | **ZIP SHA256** | \`${ZIP_SHA256}\` |
          | **Image Size** | ${IMAGE_SIZE} |
          | **Image SHA256** | \`${IMAGE_SHA256}\` |

          ## Source Commits
          | Component | Commit |
          |-----------|--------|
          | **ReSukiSU** | ${KSU_COMMIT_LINK} |
          | **SUSFS** | ${SUSFS_COMMIT_LINK} |

          ## Features Status
          - ${KPM_STATUS} KPM
          - ${SUSFS_ENABLED} SUSFS
          - ${BBR_ECN_STATUS} BBR & ECN
          - ${NETFILTER_STATUS} NETFILTER
          - ${LSM_STATUS} LSM (BBG)
          - ${UNICODE_FIX_STATUS} Unicode Bypass Fix
          EOF

      - name: 📊 Post-build Disk Check
        if: always()
        shell: bash
        run: |
          echo "::group::Disk Usage Analysis"
          df -h
          echo ""
          du -sh kernel_workspace 2>/dev/null || echo "Build directory not found"
          echo ""
          du -h kernel_workspace 2>/dev/null | sort -rh | head -10 || true
          echo ""
          if [ "${{ github.event.inputs.BUILD_NOCCACHE }}" != "true" ] && command -v ccache >/dev/null 2>&1; then
            ccache -s
          fi
          echo "::endgroup::"

      - name: 🧹 Cleanup
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          echo "::group::Cleanup Operations"

          safe_remove() {
            local path="$1"
            if [ -e "$path" ]; then
              echo "Removing: $path"
              sudo rm -rf "$path" 2>/dev/null || true
            else
              echo "Path does not exist: $path"
            fi
          }

          if [ "${{ github.event.inputs.SPACE_NOCLEAN }}" != "true" ]; then
            safe_remove "kernel_workspace/kernel_platform/common/out"
            safe_remove "kernel_workspace/kernel_platform/out"
            safe_remove "kernel_workspace/.repo"
            sudo rm -rf /tmp/* 2>/dev/null || true

            if [ "${{ github.event.inputs.BUILD_NOCCACHE }}" = "true" ]; then
              safe_remove "${GITHUB_WORKSPACE}/.ccache_${{ github.event.inputs.FILE }}"
            fi
          else
            echo "Space cleanup disabled (SPACE_NOCLEAN=true)"
          fi

          df -h
          echo "::endgroup::"

      - name: 🆘 Emergency Cleanup (if build fails)
        if: failure()
        shell: bash
        run: |
          echo "::group::Emergency Disk Cleanup"
          sudo rm -rf kernel_workspace/.repo 2>/dev/null || true
          sudo rm -rf kernel_workspace/kernel_platform/common/out/*/intermediates 2>/dev/null || true
          sudo rm -rf kernel_workspace/kernel_platform/out/*/intermediates 2>/dev/null || true
          sudo rm -rf /var/log/* /tmp/* /var/tmp/* 2>/dev/null || true
          sudo rm -rf ~/.cache/* 2>/dev/null || true
          df -h
          echo "::endgroup::"
